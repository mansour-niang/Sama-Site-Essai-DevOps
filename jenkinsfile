pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                 git credentialsId: 'github-token', git 'https://github.com/mansour-niang/Sama-Site-Essai-DevOps.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('Build') {
            steps {
                sh 'npm run build'
            }
        }
        stage('Docker Build') {
            steps {
                script {
                    dockerImage = docker.build("ton-utilisateur/ton-image-vitejs")
                }
            }
        }
        stage('Push Docker') {
            steps {
                script {
                    docker.withRegistry('https://registry.hub.docker.com', 'dockerhub-credentials') {
                        dockerImage.push("latest")
                    }
                }
            }
        }
    }
}

